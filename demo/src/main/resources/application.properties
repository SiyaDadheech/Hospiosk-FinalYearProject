# spring.application.name=demo


razorpay.key_id=${RAZORPAY_KEY_ID}
razorpay.key_secret=${RAZORPAY_KEY_SECRET}

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}


# # Configure datasource to match DBConnection.java (update password as needed)
# # If you don't want a DB, you can remove 'spring-boot-starter-data-jdbc' from pom.xml
# spring.datasource.url=jdbc:mysql://localhost:3306/hospital_queue?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# # replace with your MySQL password (example: siya@123)
# spring.datasource.password=siya1234
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # If you prefer to disable DataSource auto-config (for testing without DB),
# # uncomment the following line and restart. Note: QueueManager uses DB.
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# # Razorpay test credentials (optional). Set these to your rzp_test keys to enable real test orders.
# razorpay.key_id=rzp_test_S28kQARurj88gk
# razorpay.key_secret=vVQhQHfy6u1Nd3c01JEUCpWn
